[package]
name = "mechanicus-companion"
version = "0.9.0"
edition = "2021"
description = "Desktop companion for mechanicus - shows active agent GIFs per session"

[[bin]]
name = "mechanicus-companion"
path = "src/main.rs"

[dependencies]
eframe = { version = "0.29", default-features = true }
egui = "0.29"
image = { version = "0.25", default-features = false, features = ["jpeg"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5"
winit = "0.30"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
